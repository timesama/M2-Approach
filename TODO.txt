
Goal:
Implement a consistent application-wide footer/status message system across the software.

Context:
There is already some code that writes helpful/cute/status information at the very bottom of the GUI in the tab g_RecFID. I like this behavior and want it standardized everywhere. Find the existing implementation first and reuse its style if appropriate, instead of creating a second unrelated mechanism. THIS: 
    def _status(self, message):
        if self.parent is not None and hasattr(self.parent, "statusBar"):
            self.parent.statusBar().showMessage(message, 8000)
        logger.info(message)>>> 
Requirements:
1. Locate the existing bottom-of-window/footer/status-bar message implementation.
2. Refactor it into a single reusable helper/API if one does not already exist.
3. Add clear status/footer messages throughout the application for user-visible actions, especially:
   - file loading
   - file saving
   - table updates
   - fitting/analysis actions
   - plotting actions
   - import/export actions
   - errors and recoverable warnings
   - long-running operations
   - successful completion of user actions
4. Keep messages concise, friendly, and informative.
5. Do not spam the footer during tight loops or repeated internal updates.
6. Prefer messages like:
   - "Loaded 4 files."
   - "Fit completed."
   - "Plot updated."
   - "No rows selected."
   - "Saved analysis results."
   - "Could not process file: <short reason>"
7. Do not use modal popups where a footer/status message is enough.
8. Do not hide real errors. Exceptions should still be logged or handled properly.
9. If the application already has logging, keep logging for developer details and use the footer for user-facing summaries.
10. Make sure all tabs/controllers use the same footer/status mechanism instead of each implementing its own.
11. If useful, add a small helper method such as:
    - set_status_message(...)
    - show_status(...)
    - notify_user(...)
   but choose names consistent with the existing codebase.
12. Avoid large unrelated refactors.

Validation:
- Run/search for all direct status bar/footer/message usages and consolidate them where reasonable.
- Ensure the app still launches.
- Ensure no imports are broken.
- Ensure existing tab workflows still work.
- Summarize which files were changed and where footer messages were added.

----
Goal:
Find unused files, functions, classes, methods, imports, constants, and dead branches of code, then remove only the parts that are safe to remove.

Important:
Be conservative. Do not delete code unless there is strong evidence it is unused. This is a GUI/scientific analysis application, so some functions may be referenced dynamically by Qt signals, callbacks, object names, UI files, or string-based dispatch.

Process:
1. Inspect the full repository structure.
2. Identify unused:
   - Python files/modules
   - classes
   - functions/methods
   - imports
   - constants
   - obsolete commented-out code
   - duplicate helper functions
3. Before deleting, verify references with repository-wide search.
4. Check for dynamic usage patterns, including:
   - Qt signal/slot connections
   - getattr/setattr
   - objectName references
   - .ui files
   - callbacks
   - menu/action connections
   - string-based method lookup
   - imports from __init__.py
5. Remove unused imports and obviously dead code first.
6. Remove unused functions/classes only when clearly safe.
7. If something looks unused but risky, do not delete it. Instead, leave a short note in the PR summary under "Potentially unused but kept".
8. Do not change behavior intentionally.
9. Do not perform broad architectural refactors in this PR.
10. Do not rename public methods/classes unless absolutely necessary.
11. Keep the diff focused on dead-code cleanup.

Validation:
- Run available tests, linters, or at minimum python import/compile checks if available.
- Run:
  python -m compileall .
- Fix any compile/import errors introduced by the cleanup.
- Summarize:
  - deleted files
  - deleted functions/classes
  - removed imports
  - risky items intentionally kept
  - validation performed
