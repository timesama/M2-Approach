TODO list:
Grouping is working only when X axis is filled.
refactor reading T1 and T2 tab - hahahahahahahahah
Icons for buttons
Units for values!
add SE FID and MSE processing as some sort of loading
___________________

TODO, Codex: Next task: focused cleanup and robustness pass for DQ temperature table and legacy FFC/.sef handling.

Do not redesign the application architecture in this task. Do not change numerical algorithms except where needed to handle failed fits safely. Keep the task focused.

TASK 1 — Fix table_DQ_2 cell behavior

Problem:
In table_DQ_2, some cells appear as “double boxes” / editable widget-like cells for some reason. They should be normal QTableWidget cells.

Required behavior:
- table_DQ_2 should use regular QTableWidgetItem cells, not embedded widgets/spinboxes/combo boxes.
- The user should not be able to edit table_DQ_2 manually.
- The table should still be selectable/copyable if current app behavior allows table copying.

Implementation:
- Inspect all code that fills table_DQ_2.
- Remove any use of setCellWidget for table_DQ_2.
- Ensure values are inserted with QTableWidgetItem.
- Set the table itself to non-editable, preferably once during setup or controller initialization:

    self.ui.table_DQ_2.setEditTriggers(QTableWidget.NoEditTriggers)

- If needed, also clear editable flags on individual items:

    item.setFlags(item.flags() & ~Qt.ItemIsEditable)

- Do not make other unrelated tables non-editable unless already intended.

Likely files:
- controllers/dq_temp_controller.py
- main_window.py, if table setup still happens there
- any shared table helper

Validation:
- Search for table_DQ_2.setCellWidget. There should be none unless there is a very deliberate reason.
- table_DQ_2 cells should render as normal table cells.
- table_DQ_2 cells should not be editable by double-clicking.

TASK 2 — Make DQ temperature fitting robust per file

Problem:
When DQ temperature comparison is loading multiple files, if fitting fails or invalid values appear for one file, the whole upload/process fails and the next files are not added/processed.

Desired behavior:
- One bad/corrupt/unfittable file should not stop the entire queue.
- Continue processing the remaining files.
- Show/log a warning for the bad file.
- Populate failed fit values safely, preferably with 0 or NaN internally.
- Do not plot invalid failed-fit points as real results.

Recommended behavior:
- For table_DQ_2, if a fit fails for one file, fill that file’s computed columns with 0.0 or an empty value, but do not abort processing the next files.
- Prefer storing failed numeric values as np.nan internally where possible.
- When writing to the visible table, display "0" or "Fit failed" consistently.
- When plotting summary points, ignore invalid entries:
    - skip NaN
    - skip None
    - skip non-finite values
    - optionally skip 0 if 0 is used as the failure sentinel

Important:
If 0 is a physically meaningful possible value, prefer np.nan internally and display "0" or "Fit failed" only in the UI. But if the current plotting/table code is easier to stabilize with 0, use 0 and explicitly skip 0 values in the summary plots.

Implementation guidance:
- In DQTempTabController.update_DQ_comparison() and update_DQ_comparison_plot(), isolate errors per file.
- Wrap each file’s fitting block in try/except so a failure affects only that row/file.
- Do not wrap the entire multi-file loop in a single broad exception that aborts all files.
- Add a helper if useful:

    def _safe_float(value, default=np.nan):
        ...

    def _is_valid_fit_value(value):
        return value is not None and np.isfinite(value) and value != 0

- When plotting center/FWHM arrays, filter invalid values before plotting.
- If arrays have mismatched lengths after filtering, build x/y pairs and filter together, e.g.:

    points = [
        (x, y)
        for x, y in zip(comparison_par, center_g)
        if np.isfinite(x) and np.isfinite(y) and y != 0
    ]

- Log warnings with file names when fitting fails:

    logger.warning("DQTemp fit failed for %s: %s", filename, exc)

- Show one user-facing warning at the end if possible, instead of many modal popups:
    “Some files could not be fitted and were filled with 0 / skipped in plots. See log for details.”

Preferred:
Avoid many QMessageBox popups inside loops. They interrupt batch processing. Accumulate failures and show one summary message after processing.

Validation:
- Loading 10 DQ comparison files where 1 file cannot be fitted should still load/process the other 9 files.
- The failed row should not crash the process.
- The failed row should not create bogus plotted points.
- The log should identify which file failed.
- The user should see a concise warning summary if there were failures.

TASK 3 — Remove legacy FFC/.sef “bad_code” pathway

Problem:
There is legacy FFC/.sef support with functions/state like:
- bad_code_makes_more_bad_code
- state_bad_code
- special .sef parsing in T1/T2
- save/load behavior connected to the old FFC/.sef path

I no longer want to support .sef analysis or saving/loading this legacy FFC mode.

Required behavior:
- Remove the legacy “bad code” / FFC / .sef analysis pathway.
- Remove state_bad_code or equivalent state connected only to .sef legacy handling.
- Remove bad_code_makes_more_bad_code if it only exists for legacy .sef/FFC.
- Remove .sef parsing from T1T2 analysis.
- If a user tries to load .sef files, show a clear QMessageBox warning and abort that load operation gracefully.

User-facing warning text:
    Legacy FFC .sef files are no longer supported.

    Please convert the data to the current supported table/text/CSV format and try again.

Do not crash.
Do not silently ignore.
Do not attempt to parse .sef.

Implementation details:
- In T1T2TabController.update_T12_table(), detect .sef early:

    if any(file.lower().endswith(".sef") for file in selected_files):
        QMessageBox.warning(...)
        return

- Remove the old .sef branch that parsed Magnetization/Profile files.
- Remove process_files if it was only used for .sef zone/frequency parsing.
- Remove state_bad_code from MainWindow/AppState if it only supported .sef.
- Remove references like:

    if self.parent.state_bad_code:
        ...

or replace with normal non-.sef behavior.
- Remove save/load compatibility logic that depends on state_bad_code.
- Search the repo for:
    state_bad_code
    bad_code_makes_more_bad_code
    .sef
    FFC
    process_files
  and remove or simplify obsolete code.

Important:
Do not remove support for normal .txt, .csv, or .dat T1/T2 files.
Only remove legacy .sef/FFC behavior.

Validation:
- Loading .sef files shows the warning and does not crash.
- Loading normal T1/T2 .txt, .csv, .dat files still works.
- Searching for state_bad_code should return no active code references.
- Searching for bad_code_makes_more_bad_code should return no active code references.
- Searching for ".sef" should only show the early unsupported-file warning/detection, not old parsing logic.
- App compiles.

TASK 4 — Keep this focused

Do not:
- Rewrite DQTemp fitting algorithms beyond safe exception handling.
- Redesign controller architecture.
- Rename Qt Designer widgets.
- Change table_SE or table_DQ behavior.
- Add new UI elements unless necessary.
- Add excessive modal popups in loops.

Run validation:

    python -m py_compile main.py main_window.py controllers/*.py dialogs/*.py utils/*.py

Then run/search:

    grep -R "table_DQ_2.setCellWidget" .
    grep -R "state_bad_code" .
    grep -R "bad_code_makes_more_bad_code" .
    grep -R ".sef" .
    grep -R "process_files" .

Expected:
- No table_DQ_2.setCellWidget usage.
- No active state_bad_code references.
- No active bad_code_makes_more_bad_code references.
- .sef appears only in unsupported-file warning/detection.
- process_files is removed if it was only for .sef.

Commit message:

    Harden DQ temperature loading and remove legacy SEF support

Proceed with code changes. Do not return only a plan.


______________
TODO: implement code from the DQMQ analysis.

__________
TODO: Implement BuildUp FID 